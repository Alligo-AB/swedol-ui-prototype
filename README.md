# swedol-ui-prototype

Simple static UI prototype repo for design sharing.

## Local preview
Open `index.html` directly in a browser, or run a simple local server.

Example:
```bash
python3 -m http.server 8080